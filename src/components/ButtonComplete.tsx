import React from "react";

const ButtonComplete = () => {
  const completeButtonHandler = () => {

  }

  return (
    <button className="w-full bg-primary-500 text-white py-1.5 mb-5 rounded" onClick={completeButtonHandler}>완료</button>
  )
}

export default ButtonComplete