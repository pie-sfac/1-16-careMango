import { atom } from 'recoil';

export const showModalState = atom({
  key: 'showModalState',
  default: false
});

export const counselingRecordsState = atom({
  key: 'counselingRecordsState',
  default: '',
});


